language: python
sudo: false

cache:
  - pip
  - apt

addons:
  apt:
    packages:
      - build-essential
      - python-dev
      - libc6
      - g++
      - gfortran
      - hdf5-tools

python:
  - 2.7

install: pip install h5py
script: py.test .
